<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.tulsivanol.android.quotes"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

  <application
      android:name="com.tulsivanol.android.quotes.QuoteApplication"
      android:allowBackup="false"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/AppTheme"
      tools:ignore="GoogleAppIndexingWarning">
    <activity
        android:name="com.tulsivanol.android.quotes.ui.AddEditActivity"
        android:parentActivityName="com.tulsivanol.android.quotes.ui.MainActivity" />
    <!-- Main Activity -->
    <activity
        android:name="com.tulsivanol.android.quotes.ui.MainActivity"
        android:theme="@style/SplashTheme">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>

</manifest>